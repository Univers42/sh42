diff <(echo "hello") <(echo "hello")
diff <(echo "hello") <(echo "world")
diff <(sort file1.txt) <(sort file2.txt)
grep root <(cat /etc/passwd)
wc -l <(ls /bin)
cat <(ls) <(pwd)
sed 's/foo/bar/' <(echo "foo baz")
paste <(seq 5) <(seq 6 10)
column -t <(ls -l) <(ls -lh)
while read line; do echo "-> $line"; done < <(ls)
diff <(env) <(env | sort)
echo "hello world" > >(tr a-z A-Z)
echo "logging this" > >(tee log.txt)
ls /nope 2> >(cat >&2)
command ls / /nope > >(grep bin) 2> >(grep denied)
echo "data" > >(tee file1.txt) > >(tee file2.txt)
grep root <(ps aux)
diff <(date) <(sleep 1; date)
gzip <(ls -l)
tar -tf <(tar -cf - /etc)
diff <(ls dir1) <(ls dir2)
awk '{print NR, $0}' <(echo -e "a\nb\nc")
cat <(seq 3) <(seq 4 6)
xargs -n1 echo < <(seq 5)
echo "abc123" > >(sed 's/[0-9]//g')
echo "syslog test" > >(logger
wc -w <(echo "one two three")
head <(curl -s https://example.com)
diff <(jq . a.json) <(jq . b.json)
mapfile -t arr < <(seq 5)
exec 3< <(ls)
read line <&3
echo "hello" > >(cat) > >(wc -c)
tail -f <(ping -c 5 google.com)
grep error <(journalctl -n 100)
diff <(sha256sum file1) <(sha256sum file2)
grep port <(echo -e "host=localhost\nport=8080")
cmd > >(tee out.log) 2> >(tee err.log >&2)
sudo diff <(ls /root) <(ls /home)
psql dbname < <(echo "SELECT 1;")
diff <(lscpu) <(cat /proc/cpuinfo)
grep bash <(cat /etc/shells) <(env)
echo "ping" > >(nc localhost 1234)
diff -u <(seq 5) <(seq 6)
watch diff <(df) <(df)
echo "1 2 3" > >(awk '{print $1+$2+$3}')
sort -m <(sort a.txt) <(sort b.txt)
set -x > >(tee debug.log)
diff <(grep -v '^#' conf1) <(grep -v '^#' conf2)
cat <(cat <<EOF
hello
world
EOF
)
vim <(echo "temporary file content")

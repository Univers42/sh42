# Auto-configuration of project
like bash automatize the configuration of its own existence. We will create a set rule of configuration

those auto configuration with macros will help our project to compile with specific features. By default all features are compiled altogether
# ALIAS
# 

